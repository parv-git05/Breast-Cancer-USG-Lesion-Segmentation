# Project Guidelines

## Folder Structure Maintenance
- The project maintains a complete text representation of its directory tree in `project_structure.txt`.
- Whenever a new file or directory is created, deleted, or relocated, run `python update_folder_structure.py` to refresh `project_structure.txt`.
